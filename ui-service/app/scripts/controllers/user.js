'use strict';

/**
 * @ngdoc function
 * @name uiServiceApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the uiServiceApp
 */
angular.module('uiServiceApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
